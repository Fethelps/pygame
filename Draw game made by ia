import pygame
import sys

# Inicializa o Pygame
pygame.init()

# Definir tamanho da janela
screen = pygame.display.set_mode((800, 600))
pygame.display.set_caption('Jogo de Desenhar')

# Cores
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)

# Inicializar variáveis
drawing = False
last_pos = None
radius = 5

# Função para desenhar
def draw_line(screen, start, end, width):
    pygame.draw.line(screen, BLACK, start, end, width)

# Preencher a tela com branco
screen.fill(WHITE)
pygame.display.flip()

# Loop principal do jogo
while True:
    for event in
